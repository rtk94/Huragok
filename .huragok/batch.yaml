# Huragok — Current Batch
#
# A batch is a list of tasks Huragok will execute against this repo.
# This is a scaffold placeholder — empty tasks, placeholder metadata —
# that validates cleanly against the Slice A schemas. Populate it
# before starting a batch, or let `huragok submit` / `huragok init-batch`
# scaffold one for you (Slice B / ADR-0007).
#
# Schema: orchestrator.state.schemas.BatchFile (ADR-0002 D3)

version: 1
batch_id: scaffold
created: 2026-04-21T00:00:00Z
description: "Placeholder — no batch in flight. Overwrite or `huragok submit` a real batch."
budgets:
  wall_clock_hours: 12
  max_tokens: 5_000_000
  max_dollars: 50.00
  max_iterations: 2
  session_timeout_minutes: 45
notifications:
  telegram_chat_id: null
  warn_threshold_pct: 80
tasks: []
